# -*- coding: utf-8 -*-
"""COSE362 기계학습 U1~U6 콘텐츠 — 전공서적급 개편(2026-09-03).
개념=새 판(바텀업·한영병기·LaTeX·중요도 칩·시험 연결), 유도·예제=content_base.py 보관분 조립."""

from content_base import UNITS as _BASE, JS, EX

_old = {u["id"]: u for u in _BASE}

COURSE = { "id":"course",
  "bigpicture": r"""
<p><b>이 과목의 단 하나의 물음</b> — 기계가 데이터로부터 "배웠다"는 것은 정확히 무엇이고,
배운 것이 <b>새 데이터에서도 통한다</b>(일반화<span class="en">(generalization)</span>)는 보장은 어디서 오는가.
중간범위는 그 물음을 다섯 갈래로 심문한다.</p>
<p>① <b>학습의 정형화 (U1)</b>: 학습=⟨과제 T, 성능 P, 경험 E⟩. 가설공간을 좁히는 <b>귀납 편향</b> 없이는
일반화 자체가 불가능하다는 정리(무편향 학습 불가)가 과목 전체의 철학적 기초다. PAC 부등식이 "편향의 보상"을 정량화한다.</p>
<p>② <b>첫 학습기 — 회귀 (U2)</b>: 최소제곱 \(w=S_{xy}/S_{xx}\) 폐형식과 경사하강 두 길.
학습률·수렴 조건 \(|1-2\eta a|<1\), 그리고 과적합↔규제(릿지)의 첫 대면.</p>
<p>③ <b>심판 (U3)</b>: 혼동행렬에서 파생되는 지표 5종, ROC·AUC, 교차검증과 데이터 누수 —
"무엇으로 이겼다고 선언할 것인가"의 규율. 모든 이후 모델은 이 심판대 위에 선다.</p>
<p>④ <b>결정트리 (U4→U5)</b>: 정보이론(엔트로피·정보이득)으로 분할을 고르는 ID3(U4),
지니로 고르는 CART와 과적합을 다스리는 가지치기(U5). 손계산 시험의 최대 지분 — 미첼 [9+,5−] 표가 성지다.</p>
<p>⑤ <b>확률적 학습 (U6)</b>: 베이즈 정리로 "가장 그럴듯한 가설"을 고르는 MAP,
조건부 독립이라는 대담한 가정으로 실전형이 된 나이브 베이즈+라플라스 평활.</p>
<div class="mapline">U1 학습이란(편향·PAC) → U2 회귀(폐형식·경사하강) → U3 평가(혼동행렬·CV) → U4 트리 I(엔트로피·IG) → U5 트리 II(지니·가지치기) → U6 베이즈·NB</div>
<p><b>시험 전략</b> — 손계산 중심(정보이득·지니·NB 표·혼동행렬)+개념 판별. 반복 축:
<b>[계산]</b> IG 소수 셋째 자리까지, 혼동행렬→지표 5종, NB 라플라스 표, 최소제곱 표 계산.
<b>[개념]</b> 편향 없이는 일반화 없다, 과적합 진단(훈련·검증 격차), 누수 사례 판별, MAP vs ML.
<b>[서술]</b> 무편향 학습 불가 논증, 교차검증 절차. 계산 문제는 "표 만들기"가 절반이다 — 열 설계를 훈련하라.</p>""" }

CONCEPTS = {

"u1": r"""
<div class="bigpic"><span class="bt">U1 큰그림</span>
"학습"을 공학 문제로 바꾸는 단원이다. 정의(T·P·E) → 가설공간이라는 무대 → 그 무대를 줄이는 귀납 편향 →
편향이 없으면 일반화도 없다는 충격적 정리 → 편향의 보상을 정량화하는 PAC 부등식.
버전공간과 후보 제거 알고리즘은 "데이터와 일치하는 가설 전부"를 추적하는 사고 실험으로,
과적합(U2·U5)·평가(U3)의 문제의식이 전부 여기서 발아한다.</div>

<h4>1. 학습의 정의와 과제 정형화 <span class="en">(Well-Posed Learning: T, P, E)</span><span class="tagm">계산 필수</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — "스팸을 배운다"는 말은 공학이 아니다. 무엇을(T), 무엇으로 재고(P), 무엇에서 배우나(E)를 못 박아야 시스템이 된다.</div>
<p><b>정의(미첼)</b> — 프로그램이 경험 E로부터, 성능 지표 P로 잰 과제 T의 성적이 오르면 "학습한다".
스팸 필터라면 T=분류, P=정밀도/재현율(U3), E=라벨된 메일. 문제를 받으면 <b>입력 표현·출력 공간·성능 지표부터 명시</b>하는 것이 문법이다.
갈래: 지도학습<span class="en">(supervised)</span>(라벨 쌍 — 분류는 이산 출력, 회귀는 연속 출력),
비지도<span class="en">(unsupervised)</span>(구조 발견), 강화<span class="en">(reinforcement)</span>(지연 보상).</p>
<div class="exambox"><span class="xt">시험 연결</span>[개념] 사례→T·P·E 분해, 분류/회귀/군집 판별. [함정] P를 "정확도"로 뭉개는 선지(불균형이면 오답 — U3 예고).</div>

<h4>2. 가설공간과 귀납 편향 <span class="en">(Hypothesis Space &amp; Inductive Bias)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 불리언 특징 n개의 "모든 함수"는 \(2^{2^n}\)개(n=4에 65536) — 데이터 몇 개로는 그중 하나를 고를 수 없다.
후보를 미리 줄여야 학습이 시작된다.</div>
<p><b>개념 등장</b> — 학습기가 고려하는 후보 집합이 가설공간<span class="en">(hypothesis space)</span> \(H\),
그것을 줄이는 사전 가정이 <b>귀납 편향</b><span class="en">(inductive bias)</span>이다.
연언(AND) 개념만 허용하면 \(|H|=3^n+1\)(특징마다 0/1/무관 — 다항!)로 붕괴 — 탐색 가능해진다.</p>
<p><b>무편향 학습 불가 정리</b> — 편향 없이 전체 함수 공간을 쓰면, 미관측 입력에 대해 +로 찍는 가설과 −로 찍는 가설이
훈련 데이터와 <b>똑같이 일치한 채</b> 항상 짝으로 남는다 → 투표는 영원히 동수 → 새 입력 예측 불가.
∴ <b>일반화하는 모든 학습기는 가정을 갖는다</b> — "어떤 편향이 이 문제에 맞는가"가 모델 선택(U2 다항 차수, U5 트리 깊이)의 진짜 이름이다.</p>
<p class="intuit">💡 <b>직관</b> — 데이터는 후보를 지울 뿐, 후보 목록은 편향이 만든다. 목록이 무한 잡탕이면 지워도 지워도 잡탕이다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 3ⁿ+1 vs 2^{2ⁿ} 크기 비교. [서술] 무편향 불가 논증(u1-정석 1.2가 골격 — 짝 가설 논법을 자기 말로).</div>

<h4>3. 개념학습과 버전공간 <span class="en">(Concept Learning &amp; Version Space)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 데이터와 일치하는 가설이 하나가 아니다. "아직 살아 있는 후보 전부"를 효율적으로 표현·갱신할 수 없나?</div>
<p><b>개념</b> — 버전공간<span class="en">(version space)</span> = 훈련 데이터와 일치하는 가설 전부.
일반성 순서를 이용해 양 끝만 저장한다: S(최특수 경계<span class="en">(most specific)</span>)와 G(최일반 경계).
후보 제거<span class="en">(candidate elimination)</span>의 리듬은 두 줄이다:</p>
<p>\[ \text{양성 예제} \Rightarrow S\text{를 최소 일반화},\qquad
\text{음성 예제} \Rightarrow G\text{를 최소 특수화} \]</p>
<p>(반대쪽 경계와 모순되는 가설은 삭제.) S=G로 만나면 수렴, 그 사이가 남으면 "확신 못 함"의 영역 —
다수결/기권으로 예측한다. 노이즈 한 방에 버전공간이 통째로 빌 수 있다는 취약점이 과적합 서사의 첫 장면이다.</p>
<p class="intuit">💡 <b>직관</b> — S는 "본 것만 인정하는 보수파", G는 "안 걸리면 다 허용하는 진보파". 데이터가 쌓이며 양쪽이 조여든다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 예제 3~4개 처리하며 S·G 추적(필수예제 1-1형 — 순서 실수 주의). [개념] 양성이 S를, 음성이 G를 움직인다(역방향 선지가 함정).</div>

<h4>4. PAC 학습 — 편향의 보상 정량화 <span class="en">(PAC Learning)</span><span class="tagm">계산 필수</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — "대충 맞는(오차≤ε) 가설을 높은 확률(≥1−δ)로" 얻으려면 표본이 몇 개 필요한가 — 학습의 가격표를 달라.</div>
<p><b>부등식</b> — 유한 가설공간의 일치 학습기<span class="en">(consistent learner)</span>에 대해:</p>
<p>\[ m\ \ge\ \frac{1}{\varepsilon}\Big(\ln|H|+\ln\frac{1}{\delta}\Big) \]</p>
<p>표본 복잡도가 \(|H|\)의 <b>로그</b>에만 의존 — 편향으로 \(|H|\)를 줄인 보상이 여기서 정량화된다
(연언 공간이면 \(\ln(3^n+1)\approx n\ln3\): 특징 수에 선형!). 읽는 법: ε(정확도)에 반비례, δ(신뢰)엔 로그 —
"더 정확히"가 "더 확실히"보다 비싸다. 과적합의 정보이론적 얼굴: |H|가 크면 우연히 데이터에 맞는 나쁜 가설이 살아남을 확률이 커진다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] m 대입 계산(필수예제 1-2형: ln 값 주어짐). [개념] 각 항의 역할(ε 반비례·δ 로그·|H| 로그) 해석 선지.</div>

<h4>5. 과적합의 씨앗 <span class="en">(Overfitting Preview)</span><span class="tagc">개념 이해용</span></h4>
<p>노이즈 낀 데이터에 완벽 일치=잡음 암기. 진단 지표는 <b>훈련↔시험(검증) 성능 격차</b> —
격차 크면 과적합(분산 문제), 둘 다 낮으면 과소적합(편향 문제). 이 이분법이 U2(차수·규제)·U3(검증 규율)·U5(가지치기)를
하나의 서사로 꿴다: 이 과목은 사실 "과적합과의 전쟁사"다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 훈련/시험 성적 표→진단→처방 고르기(필수예제 1-3형). </div>""",

"u2": r"""
<div class="bigpic"><span class="bt">U2 큰그림</span>
첫 실전 학습기. "직선 하나 긋기"에 과목의 모든 주제가 압축돼 있다 — 손실 정의(잔차 제곱합) → 최적화 두 경로
(폐형식 vs 경사하강) → 표현력 확장(다항 특징) → 그 대가(과적합) → 억제(릿지 규제) → 선택 기준(검증, U3).
경사하강 수렴 조건은 자동제어의 안정성·마르코프 수렴과 같은 "등비 감쇠" 구조다 — 공대 전 과목을 관통하는 패턴.</div>

<h4>1. 최소제곱 — 손실을 정의하고 미분으로 끝내기 <span class="en">(Least Squares)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 점 n개에 "가장 잘 맞는" 직선 \(\hat y=wx+b\)? "잘 맞는다"부터 수식이어야 한다.</div>
<p><b>정의와 해</b> — 잔차 제곱합 \(J(w,b)=\sum_i(y_i-wx_i-b)^2\)을 최소화. 볼록<span class="en">(convex)</span>이라
1계 조건이 전역 최소를 준다:</p>
<p>\[ w=\frac{S_{xy}}{S_{xx}}=\frac{\sum(x_i-\bar x)(y_i-\bar y)}{\sum(x_i-\bar x)^2},\qquad b=\bar y-w\bar x \]</p>
<p>회귀선은 \((\bar x,\bar y)\)를 지난다. 잔차의 성질 — \(\sum e_i=0\), \(\sum x_ie_i=0\)(직교) — 이 공짜 검산기.
품질은 결정계수:</p>
<p>\[ R^2=1-\frac{SSE}{SST} \]</p>
<p>— "평균 예측 대비 설명한 분산 비율". R²=0이 기준선(평균 모델), 시험셋에선 음수도 가능(평균보다 못함).</p>"""
+JS("2.1", r"손계산 표 4열: \(x-\bar x\ |\ y-\bar y\ |\ \text{곱}\ |\ \text{제곱}\). 4~5점 문제는 이 표 하나로 w·b·R²까지 3분. 검산은 잔차 합=0. 표 설계가 곧 점수다 — 시험지에 표부터 그려라.")
+r"""
<div class="exambox"><span class="xt">시험 연결</span>[계산] 표 계산→w·b→R²(필수예제 2-1·2-2형). [개념] (x̄,ȳ) 통과, R²의 뜻과 음수 가능성.</div>

<h4>2. 경사하강 — 미분을 못 풀 때의 만능 경로 <span class="en">(Gradient Descent)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 폐형식이 없는 손실(로지스틱, 신경망)이 온다. "기울기 반대로 조금씩"이라는 범용 절차가 필요하다.</div>
<p><b>절차</b> — \(w\leftarrow w-\eta\nabla J(w)\), \(\eta\)=학습률<span class="en">(learning rate)</span>.
2차 손실 \(J=aw^2\)에서 갱신은 \(w\leftarrow(1-2\eta a)w\) — 등비수열이라 수렴 조건이 즉시 나온다:</p>
<p>\[ |1-2\eta a|<1\iff 0<\eta<\frac{1}{a} \]</p>
<p>\(\eta\) 과대→발산(진동 폭 증가), \(\eta=1/2a\)→한 방, 과소→기어간다. 다차원에선 a→최대 곡률:
특징 스케일이 다르면 등고선이 길쭉해져 지그재그 — <b>표준화</b>가 처방이다.
배치/미니배치/SGD는 "한 걸음의 정확도 vs 걸음 빈도"의 트레이드오프.</p>
<p class="intuit">💡 <b>직관</b> — 안개 낀 골짜기에서 발밑 경사만 보고 내려가기. 보폭(η)이 골짜기 폭(1/a)을 넘으면 반대편 벽에 부딪힌다.</p>
<p>🔗 <b>연결</b> — (1−2ηa)ⁿ 감쇠는 자동제어 1차계·마르코프 (1−a−b)ⁿ과 동일 구조 — "고정점+등비"는 공학 공통 문법이다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 2~3스텝 손추적(필수예제 2-3형), 수렴 η 범위. [개념] 표준화가 돕는 이유, SGD의 잡음과 장점.</div>

<h4>3. 표현력과 규제 — 과적합과의 첫 전투 <span class="en">(Polynomial Features &amp; Ridge)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 직선이 못 맞추는 곡선 데이터. 특징에 \(x^2, x^3\)을 추가하면 되지만 — 어디까지?</div>
<p><b>확장과 대가</b> — 다항 특징은 <b>파라미터에 대해 선형</b>이라 여전히 "선형 모델"(폐형식·경사하강 그대로).
차수를 올리면 훈련 오차는 단조 감소하지만 검증 오차는 <b>U자</b> — 내려가다(과소적합 해소) 올라간다(잡음 암기).
억제 장치가 릿지<span class="en">(ridge regression)</span>: \(J+\lambda\|w\|^2\)로 큰 계수에 벌금 — 1변수 폐형식이 투명하다:</p>
<p>\[ w_{ridge}=\frac{S_{xy}}{S_{xx}+\lambda} \]</p>
<p>λ가 계수를 0 쪽으로 수축(편향↑ 분산↓). 진단 언어를 굳혀라: 기대 오차 = 편향² + 분산 + 잡음(바닥=베이즈 오류).
격차 크면 분산 문제(데이터·규제·단순화), 둘 다 나쁘면 편향 문제(표현력·특징). 차수·λ의 선택은 <b>언제나 검증 성능</b>(U3)으로.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 릿지 폐형식 대입, λ↑에 따른 w 수축. [개념] U자 곡선 해석, 편향-분산 진단→처방 매칭(단골 세트).</div>""",

"u3": r"""
<div class="bigpic"><span class="bt">U3 큰그림</span>
모델을 만드는 법(U2) 다음은 <b>이겼다고 선언하는 법</b>이다. 도구는 혼동행렬 하나 — 지표 5종이 전부 그 4칸의 비율이다.
문턱을 훑으면 ROC·AUC, 데이터를 아껴 쓰면 교차검증, 그리고 이 모든 것을 무효화하는 단 하나의 죄 — 데이터 누수.
"정확도 99%"가 왜 무의미할 수 있는지(불균형), 왜 시험셋은 한 번만 열어야 하는지 — 실무 사고의 대부분이 이 단원 위반에서 난다.</div>

<h4>1. 혼동행렬과 지표 5종 <span class="en">(Confusion Matrix &amp; Metrics)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 암 환자 1%인 데이터에서 "전부 정상"이라 답하면 정확도 99%. 숫자 하나로는 심판이 안 된다.</div>
<p><b>도구</b> — 예측×실제 4칸: TP·FP·FN·TN. 파생 지표:</p>
<p>\[ \text{Acc}=\frac{TP+TN}{\text{전체}},\quad
P=\frac{TP}{TP+FP},\quad R=\frac{TP}{TP+FN},\quad
F_1=\frac{2PR}{P+R},\quad \text{Spec}=\frac{TN}{TN+FP} \]</p>
<p>정밀도<span class="en">(precision)</span>는 "양성이라 했을 때 맞을 확률"(FP의 비용 — 스팸 오차단),
재현율<span class="en">(recall)</span>은 "실제 양성을 잡을 확률"(FN의 비용 — 암 놓침). 둘은 문턱을 사이에 둔 시소이고
F1은 조화평균(작은 쪽에 민감)이다. <b>어느 지표를 최적화할지는 오류의 비용이 정한다</b> — 문제 문맥→지표 선택이 개념 문항의 몸통.
베이즈 관점: 유병률이 낮으면 좋은 검사도 PPV(정밀도)가 무너진다(확률과정 U1과 같은 계산!).</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 행렬→5지표(분모 선택이 전부), 문턱 이동 시 P·R 방향. [개념] 상황→지표 매칭(암 진단=재현율, 스팸=정밀도).</div>

<h4>2. ROC와 AUC <span class="en">(ROC Curve &amp; AUC)</span><span class="tagm">계산 필수</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 문턱 하나짜리 성적표는 문턱을 바꾸면 뒤집힌다. 문턱 <b>전체</b>에서의 실력을 한 장에 그리자.</div>
<p><b>개념</b> — 문턱을 1→0으로 훑으며 (FPR, TPR)=(1−특이도, 재현율)을 찍은 궤적이 ROC.
대각선=동전 던지기, 좌상단=이상형. 곡선 아래 면적 AUC의 확률적 정의가 계산 문제의 열쇠다:</p>
<p>\[ \mathrm{AUC}=P(\text{임의 양성의 점수}>\text{임의 음성의 점수}) \]</p>
<p>— 소표본이면 양성×음성 쌍을 전부 나열해 이긴 비율(동점 0.5)로 손계산. AUC는 문턱 불변·클래스 비율 불변이라
불균형 데이터의 표준 심판이지만, "확률 보정"은 안 본다는 한계까지 알면 만점.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 쌍 비교 AUC(점수 목록 주어짐), ROC 점 찍기. [개념] AUC=0.5·1.0의 의미, 불균형에서 Acc 대신 AUC인 이유.</div>

<h4>3. 검증 규율 — 홀드아웃·교차검증·누수 <span class="en">(Validation, Cross-Validation &amp; Leakage)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 데이터가 200개뿐. 시험셋을 떼면 훈련이 부족하고, 안 떼면 성적을 못 믿는다.</div>
<p><b>규율</b> — 3분할이 원칙: 훈련(적합)/검증(하이퍼파라미터 선택)/시험(최종 1회 보고).
데이터가 아까우면 k-겹 교차검증<span class="en">(k-fold cross-validation)</span>: k조각 중 하나씩 검증을 돌려 평균 —
전 데이터가 한 번씩 심판석에 앉는다. 하이퍼파라미터 탐색까지 공정하게 하려면 중첩 CV(바깥 평가/안쪽 선택).</p>
<p><b>누수</b><span class="en">(data leakage)</span> — 시험 정보가 훈련 과정에 새는 모든 경로:
전체 데이터로 표준화 후 분할(통계량 누수), 시험셋 성능으로 모델 고르기(선택 누수), 시계열을 무작위 분할(미래 누수).
증상은 "검증 성적만 화려하고 실전에서 붕괴". <b>전처리 통계량은 훈련 폴드에서만 산출</b>이 규율의 한 줄 요약이다.</p>
<p class="intuit">💡 <b>직관</b> — 시험지는 밀봉된 채 마지막에 한 번. 밀봉을 깬 횟수만큼 성적표는 부풀려진 거짓이 된다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] k-겹 평균±편차. [개념] 누수 사례 판별(3유형), 검증 vs 시험의 역할 구분 — 서술형으로도 출제.</div>""",

"u4": r"""
<div class="bigpic"><span class="bt">U4 큰그림</span>
결정트리의 심장 — "어떤 질문(특징)이 좋은 질문인가"를 정보이론이 숫자로 답한다.
불확실성의 단위(엔트로피) → 질문 후 남는 불확실성(조건부 엔트로피) → 그 감소량(정보이득) → 최대 이득 특징으로 분할(ID3)의
4단 논법. 미첼의 [9+,5−] 테니스 표가 전 세계 시험지의 성지다 — IG(습도)=0.151 > IG(바람)=0.048을
소수 셋째 자리까지 손으로 재현하는 것이 이 단원의 통과의례.</div>

<h4>1. 엔트로피 — 불확실성의 자 <span class="en">(Entropy)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — [9+,5−] 데이터와 [7+,7−] 데이터, 어느 쪽이 "더 혼란스러운가"를 수치로 말해야 분할을 비교한다.</div>
<p><b>정의</b> — 이진 분포의 엔트로피:</p>
<p>\[ H(p)=-p\log_2 p-(1-p)\log_2(1-p)\ \ [\text{bit}] \]</p>
<p>순수(p=0,1)에서 0, 반반(p=1/2)에서 최대 1비트 — "평균 예/아니오 질문 수"로 읽는다.
손계산 상수를 몸에: \(\log_2 3\approx1.585\), \(H(0.25)\approx0.811\), \([9+,5-]\to H(9/14)\approx0.940\).
다класс는 \(H=-\sum p_i\log_2 p_i\), 최대 \(\log_2 k\).</p>
<p class="intuit">💡 <b>직관</b> — 엔트로피는 "스무고개에서 남은 평균 질문 수". 좋은 특징은 질문 수를 많이 깎는 특징이다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] H 손계산(log₂ 환산 \(\log_2x=\ln x/\ln2\)). [개념] 0·최대의 조건, 비트 해석.</div>

<h4>2. 정보이득 — 질문의 가치 <span class="en">(Information Gain)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 특징 A로 나누면 가지마다 순도가 다르게 좋아진다. "평균적으로 얼마나 좋아졌나"를 하나의 수로.</div>
<p><b>정의</b> — 분할 후 가중평균 엔트로피(조건부 엔트로피)를 빼면 이득:</p>
<p>\[ IG(S,A)=H(S)-\sum_{v}\frac{|S_v|}{|S|}H(S_v) \]</p>
<p>ID3 알고리즘 = "IG 최대 특징으로 분할"의 재귀(순수해지거나 특징 소진까지).
계산 규율: 가지별 \([p+,n-]\) 세기 → 가지 H → <b>가중치 |S_v|/|S|</b> 곱해 합 → H(S)에서 뺀다.
가중치 빼먹기가 오답 1위. IG≥0(정보는 해가 안 된다 — 등호는 독립일 때).</p>"""
+JS("4.1", r"미첼 표 재현 목표치(암기 검산용): H(S)=0.940, IG(습도)=0.151, IG(바람)=0.048, IG(전망)=0.246 → 루트는 전망(Outlook). 시험 표가 달라도 절차는 동일 — '가지 세기→H→가중합→차' 4박자를 소수 셋째 자리까지.")
+r"""
<div class="exambox"><span class="xt">시험 연결</span>[계산] 표→IG 2~3특징 비교→루트 선택(최대 배점). [함정] 가중치 누락, log₂ 대신 ln 사용, 가지 하나가 순수(H=0)일 때 계산 생략 가능.</div>

<h4>3. 다지 분할 편향과 이득비 <span class="en">(Multi-way Split Bias &amp; Gain Ratio)</span><span class="tagm">계산 필수</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — "회원 ID"로 나누면 가지마다 1명씩 — 전부 순수라 IG 최대다. 그런데 이 트리는 쓰레기다. 왜?</div>
<p><b>진단과 처방</b> — IG는 가짓수 많은 특징을 편애한다(잘게 쪼개면 순수해 보임 — 일반화는 0).
C4.5의 처방은 분할 자체의 엔트로피로 나누는 <b>이득비</b>:</p>
<p>\[ SplitInfo(A)=-\sum_v\frac{|S_v|}{|S|}\log_2\frac{|S_v|}{|S|},\qquad
GainRatio=\frac{IG}{SplitInfo} \]</p>
<p>가짓수 벌금을 물린 IG다. ID 특징은 SplitInfo가 커서(=log₂n) 이득비가 붕괴 — 편향 교정.
이 문제의식(훈련에서 좋아 보임≠일반화)이 U5 가지치기의 예고편이다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] IG는 큰데 이득비는 작은 사례 재현. [개념] 왜 다지 분할이 편애되나(서술 짧은형).</div>""",

"u5": r"""
<div class="bigpic"><span class="bt">U5 큰그림</span>
같은 트리를 다른 자(지니)로 재고 — CART는 이진 분할+지니로 ID3와 대비된다 — 트리의 고질병인 과적합을
수술(가지치기)한다. "완전히 자란 트리는 훈련 데이터의 암기"라는 진단에서, 검증 데이터로 가지를 되무르는
축소 가지치기(REP)와 벌금 α로 잎 수를 다스리는 비용-복잡도 가지치기까지 — U1의 편향 서사와 U3의 검증 규율이
트리 위에서 합류하는 단원이다.</div>

<h4>1. 지니 불순도와 CART <span class="en">(Gini Impurity &amp; CART)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 엔트로피의 log 계산이 무겁다. 순수도만 비교할 거면 더 싼 자는 없나?</div>
<p><b>정의</b> — "무작위로 뽑아 무작위 라벨을 붙일 때 틀릴 확률":</p>
<p>\[ Gini(p)=1-\sum_i p_i^2\ \xrightarrow{\text{이진}}\ 2p(1-p) \]</p>
<p>순수 0, 반반 0.5(이진 최대). 엔트로피와 개형이 거의 같아 <b>분할 순위도 대개 일치</b> — 실무(CART·sklearn 기본)는 지니.
CART의 다른 결단: 모든 분할을 <b>이진</b>으로(다지 편향 원천 차단, 연속 특징은 문턱 s로 \(x\le s\) / \(x>s\)),
분할 기준은 가중 지니 감소:</p>
<p>\[ \Delta Gini=Gini(S)-\Big(\tfrac{|S_L|}{|S|}Gini(S_L)+\tfrac{|S_R|}{|S|}Gini(S_R)\Big) \]</p>
<p>연속 문턱 후보는 정렬 후 <b>이웃 값의 중점</b>만 보면 충분하다(계산 문제의 정형). 회귀 트리는 지니 자리에 분산(MSE), 잎 예측은 평균.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 지니·ΔGini 손계산, 연속 문턱 비교. [개념] 지니 vs 엔트로피(개형·비용·실무 기본값), CART 이진의 이유.</div>

<h4>2. 과적합과 정지 규칙 <span class="en">(Overfitting &amp; Stopping Rules)</span><span class="tagm">계산 필수</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 트리를 끝까지 키우면 훈련 정확도 100% — 잎마다 표본 1개짜리 암기 사전이 된다.</div>
<p><b>진단</b> — 깊이↑ ⇒ 훈련 오차 단조 감소, 검증 오차 U자(U2 다항 차수와 같은 그림 — 같은 병이다).
사전 정지<span class="en">(pre-pruning)</span>: 최대 깊이·최소 표본·최소 이득 문턱 — 싸지만 근시안
(당장 이득 없는 분할 뒤에 좋은 분할이 숨을 수 있다 — XOR형). 그래서 정석은 "끝까지 키우고 되무르는" 사후 가지치기다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[개념] 사전 vs 사후 장단, 훈련/검증 곡선 해석. [계산] 정지 조건 적용 판정.</div>

<h4>3. 가지치기 — REP와 비용-복잡도 <span class="en">(Reduced-Error &amp; Cost-Complexity Pruning)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 어느 가지가 "암기"이고 어느 가지가 "지식"인가? 판사가 필요하다 — 훈련에 안 쓴 데이터.</div>
<p><b>REP</b><span class="en">(reduced-error pruning)</span> — 각 내부 노드에 대해 "서브트리를 잎(다수결)으로 접었을 때
<b>검증 정확도</b>가 나빠지지 않으면 접는다"를 반복(상향식). 검증 데이터가 잡음 가지를 도려낸다 — U3 규율의 트리판.</p>
<p><b>비용-복잡도</b><span class="en">(cost-complexity)</span> — 벌금식 하나로 시리즈를 만든다:</p>
<p>\[ R_\alpha(T)=R(T)+\alpha|T|\quad(|T|=\text{잎 수}) \]</p>
<p>α=0이면 완전 트리, α↑마다 "오류 증가/잎 감소" 효율이 나쁜 가지부터 접힌다 — 접히는 임계
\(\alpha^*=\frac{\Delta R}{\Delta|T|}\)(가지 하나의 가성비). α 시리즈 중 최종 선택은 교차검증(U3)으로.
λ(릿지)·α(트리)·깊이 — 전부 같은 자리(복잡도 손잡이)라는 통찰이 과목의 등뼈다.</p>
<p class="intuit">💡 <b>직관</b> — 가지치기는 "의심스러우면 단순한 쪽" — 오컴의 면도날을 검증 데이터가 집행하는 절차다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] REP 접기 판정(검증 정오표), α* 임계 계산. [개념] α의 역할=λ의 역할(연결 선지), 상향식인 이유.</div>""",

"u6": r"""
<div class="bigpic"><span class="bt">U6 큰그림</span>
학습을 확률 추론으로 다시 세운다 — "데이터가 주어졌을 때 가장 그럴듯한 가설"이 베이즈 정리의 사후확률 최대화(MAP)다.
우도까지만 보면 ML 추정. 분류에 적용하면 "클래스별 우도×사전"의 비교가 되는데, 특징이 여럿이면 결합 우도가
지수적으로 폭발한다 — 이를 조건부 독립 한 방으로 눌러버린 것이 나이브 베이즈. 라플라스 평활·로그 변환까지 얹으면
스팸 필터라는 실전기가 완성된다. 표 하나로 끝까지 손계산되는, 시험 최애 단원.</div>

<h4>1. 베이즈 정리와 MAP <span class="en">(Bayes' Theorem, MAP vs ML)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 증거 D를 보고 가설 h들의 순위를 매기고 싶다. "D를 잘 설명하는" 것과 "원래 그럴듯한" 것을 어떻게 합치나?</div>
<p><b>정리</b> —</p>
<p>\[ P(h\mid D)=\frac{P(D\mid h)\,P(h)}{P(D)}\qquad
\text{(사후}^{\text{posterior}}\propto\text{우도}^{\text{likelihood}}\times\text{사전}^{\text{prior}}) \]</p>
<p>순위 비교엔 분모 P(D)(정규화 상수) 불필요. 두 추정자:</p>
<p>\[ h_{MAP}=\arg\max_h P(D|h)P(h),\qquad h_{ML}=\arg\max_h P(D|h) \]</p>
<p>— 사전이 균등하면 둘이 일치. 데이터가 쌓일수록 우도가 사전을 압도(사전의 영향은 소표본에서 크다).
오즈×우도비<span class="en">(odds × likelihood ratio)</span> 형태로 증거를 곱해가는 갱신 감각까지 —
확률과정 U1 베이즈와 같은 수학이 "가설 선택"의 옷을 입은 것이다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 두 가설 MAP 비교(사전 곱 유무로 승자가 뒤집히는 설계). [개념] MAP vs ML, 사전의 역할.</div>

<h4>2. 나이브 베이즈 — 대담한 가정의 실전기 <span class="en">(Naïve Bayes Classifier)</span><span class="tagx">시험 핵심</span></h4>
<div class="whyq"><b>❓ 문제 상황</b> — 특징 n개의 결합 우도 \(P(x_1,\dots,x_n|c)\)를 표로 만들려면 조합이 지수적 — 데이터가 감당 못 한다.</div>
<p><b>가정과 공식</b> — 클래스가 주어지면 특징들이 서로 독립이라고 <b>가정</b>(naïve)하면 곱으로 붕괴:</p>
<p>\[ \hat c=\arg\max_c\ P(c)\prod_{i=1}^{n}P(x_i\mid c) \]</p>
<p>표 계산 절차: 클래스별 사전(빈도) → 특징별 조건부 확률(빈도) → 곱 비교. 가정은 대개 거짓이지만
<b>순위(argmax)만 맞으면 분류는 성공</b>한다는 것이 실전력의 비밀. 확률이 작아지므로 로그 합으로:
\(\log P(c)+\sum\log P(x_i|c)\) — 언더플로 방지+덧셈화.</p>
<p><b>라플라스 평활</b><span class="en">(Laplace smoothing)</span> — 훈련에 없던 값이면 \(P=0\)이 곱 전체를 살해한다. 처방:</p>
<p>\[ P(x_i\mid c)=\frac{n_{x}+1}{n_c+|V|} \]</p>
<p>(모든 칸에 가상 표본 1개 — |V|=그 특징의 값 가짓수). "본 적 없음=불가능"이라는 오만을 교정하는 사전분포의 얼굴이다.</p>"""
+JS("6.1", r"NB 표 계산 5단계: ① 클래스별 사전 P(c) ② 특징×클래스 빈도표 ③ (평활 지시 있으면) +1/+|V| ④ 곱(또는 로그 합) ⑤ 대소 비교로 판정 — 정규화해 확률로 보고하라면 합으로 나눔. 0이 하나라도 보이면 평활 여부부터 재확인: 평활 누락·이중 적용이 최다 실수.")
+r"""<p class="intuit">💡 <b>직관</b> — NB는 "증거 하나하나가 독립 증인"이라 치고 증언을 곱하는 재판. 증인이 결탁(상관)해도 판결 방향은 대개 맞는다.</p>
<p>🔗 <b>연결</b> — U1 편향 서사의 완결: NB의 조건부 독립이야말로 강한 귀납 편향이고, 그 덕에 적은 데이터로 학습된다(PAC의 정신). U3 지표로 심판받고, 트리(U4·U5)와 "해석 가능한 소형 모델" 자리를 다툰다.</p>
<div class="exambox"><span class="xt">시험 연결</span>[계산] 평활 낀 표 계산 전 과정(최대 배점 — 필수예제 6형), 로그 합 비교. [개념] 가정의 이름과 실패 조건(중복 특징), 0 확률 문제.</div>""",
}

UNITS = [COURSE] + [{**_old[k], "concept": CONCEPTS[k]} for k in ["u1","u2","u3","u4","u5","u6"]]
